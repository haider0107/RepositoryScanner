# Optional: Add your GitLab token to increase API rate limits
# and enable scanning of private repositories
# Get one at: https://gitlab.com/-/user_settings/personal_access_tokens
GITLAB_TOKEN=your_gitlab_token
